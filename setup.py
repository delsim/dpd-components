from setuptools import setup

version = "0.0.1" # TODO import this

setup(name="dpd_components", # TODO fix this
      version=version,
      author="Gibbs Consulting",
      include_package_data=True,
      description="Django Plotly Dash Components",
      install_requires=[],
      packages=['dpd_components',],
      license="AGPL",
      )
